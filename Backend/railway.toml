name = "interview-king-backend"

[build]
  buildCommand = "pip install -r requirements.txt"

[deploy]
  startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[environment]
  ENVIRONMENT = "production"
  DEBUG = "false"
  
[healthcheck]
  path = "/"